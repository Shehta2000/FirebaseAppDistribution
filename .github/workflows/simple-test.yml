name: Flutter CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.2'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # Step 4: Run tests
      - name: Run tests
        run: flutter test

      # Step 5: Build Android APK (Debug)
      - name: Build Android APK
        run: flutter build apk --debug

      # Step 6: Build Flutter Web (Optional)
      - name: Build Flutter Web
        run: flutter build web --release

      # Step 7: Show build information
      - name: Show build info
        run: |
          echo "🎉 Build completed successfully!"
          echo "✅ Flutter Version: $(flutter --version | head -1)"
          echo "✅ Dependencies: Installed"
          echo "✅ Tests: Passed"
          echo "✅ Android APK: Built"
          echo "✅ Web Build: Built"
          if [ -f "build/app/outputs/flutter-apk/app-debug.apk" ]; then
            APK_SIZE=$(stat -c%s build/app/outputs/flutter-apk/app-debug.apk)
            echo "📱 APK Size: $(($APK_SIZE / 1024 / 1024)) MB"
          fi
          echo "🚀 Ready for deployment!"

      # Future: Deploy to Firebase App Distribution
      # - name: Deploy to Firebase App Distribution
      #   uses: wzieba/Firebase-Distribution-Github-Action@v1
      #   with:
      #     appId: ${{ secrets.FIREBASE_ANDROID_APP_ID }}
      #     serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      #     groups: testers
      #     file: build/app/outputs/flutter-apk/app-debug.apk

      # Future: Deploy Web to Firebase Hosting
      # - name: Deploy to Firebase Hosting
      #   uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: ${{ secrets.GITHUB_TOKEN }}
      #     firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
      #     projectId: flutter-app-distribution-d11eb
